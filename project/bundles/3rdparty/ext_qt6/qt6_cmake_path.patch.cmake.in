diff --git a/qtbase/configure b/qtbase/configure
index e57707dc0b..3725c257b2 100755
--- a/qtbase/configure
+++ b/qtbase/configure
@@ -163,6 +163,6 @@ if [ -n "$CFG_TOPLEVEL" ]; then
     cd ..
 fi
 
-cmake "-DOPTFILE=${optfilepath}" ${top_level_arg} ${fresh_requested_arg} -P "${relpath}/cmake/QtProcessConfigureArgs.cmake"
+/opt/cmake/bin/cmake "-DOPTFILE=${optfilepath}" ${top_level_arg} ${fresh_requested_arg} -P "${relpath}/cmake/QtProcessConfigureArgs.cmake"
 
 IFS=$SAVED_IFS
